const Discord = require("discord.js");
const prettyMs = require("pretty-ms");
module.exports = {
  name: "guildMemberAdd",
  disabled: false,
  run: async (client,member) => {
    //Code here
    
  }
}
